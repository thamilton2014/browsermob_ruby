require 'bundler'
Bundler::GemHelper.install_tasks

require 'rspec/core/rake_task'

namespace :spec do
  desc 'Run unit specs.'
  puts "[Pending]"
end

desc 'Run all specs.'
task :spec => %w[spec:unit]

task :default => :spec